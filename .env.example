# Copy this to .env.local for local dev, or set these in Vercel's
# Project Settings -> Environment Variables for production.

# The live backend URL (deployed on FastAPI Cloud):
VITE_API_BASE_URL=https://grid-senseai-backend.fastapicloud.dev

# Set to the literal string "true" only for offline demo mode. Any other
# value (including unset) uses the live backend. Failed live requests must
# show an error — they do not fall back to mocks.js.
VITE_USE_MOCK_DATA=false
